# Runtime dependencies. Versions are the ones the test suite runs against;
# lower bounds are the oldest known to work.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
redis>=5.0.0
structlog>=24.1.0
asgi-correlation-id>=4.3.0
httpx>=0.27.0              # Ollama, Open-Meteo, ingestion clients

# Geo and data
geoalchemy2>=0.20.0        # PostGIS column types
psycopg[binary]>=3.3.0     # offline POI loader, migrations tests
pgvector>=0.3.0            # knowledge embeddings column type
geographiclib>=2.0         # geodesic envelope tests (WGS84)
shapely>=2.0               # region polygons
rapidfuzz>=3.6             # fuzzy name matching
pyyaml>=6.0                # data/config/*.yaml

# Planning
ortools>=9.10              # CP-SAT solver
