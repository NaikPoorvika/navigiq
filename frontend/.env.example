# Where `npm run dev` / `npm run preview` proxy /api. The FastAPI server.
# (Port 8000 on this workstation belongs to another service; NavigIQ uses 8010.)
NAVIGIQ_API_PROXY=http://127.0.0.1:8010
# Playwright E2E base URL (the running Vite dev or preview server).
E2E_BASE_URL=http://127.0.0.1:5173
